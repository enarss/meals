import { TextField } from "@mui/material"

const SearchBar = () => {
    return (
        <TextField variant="outlined" label="Search" size="small" sx={{width: "25%"}} />
    )
}

export default SearchBar